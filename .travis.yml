addons:
  postgresql: 11.1
before_script:
  - cp config/database.yml.travis config/database.yml
dist: trusty
language: ruby
rvm:
  - 2.5.1
script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec
services:
  - postgresql